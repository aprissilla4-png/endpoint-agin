/* ======================================= */
/* A-GIN TRAVEL v4.0 - FINAL & UNIFIED CSS */
/* ======================================= */

@import url('https://fonts.googleapis.com/css2?family=Lexend:wght@500;700&family=Inter:wght@400;500;600&display=swap');

/* Keyframes untuk animasi */
@keyframes aurora-gradient {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
}
@keyframes subtle-float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-8px); }
}
@keyframes ripple {
    to {
        transform: scale(4);
        opacity: 0;
    }
}
@keyframes fade-in {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
}

:root {
    --font-display: 'Lexend', sans-serif;
    --font-body: 'Inter', sans-serif;
    
    /* Palet Warna Baru: Violet & Teal yang Energetik */
    --primary-rgb: 109, 40, 217; /* Untuk digunakan di rgba() */
    --primary: #6d28d9;
    --primary-light: #a78bfa;
    --primary-dark: #4c1d95;
    --primary-foreground: #ffffff;
    
    --secondary: #0d9488; /* Teal */
    --success: #16a34a;
    --danger: #dc2626;

    /* Tema Light */
    --background: #f0f2f5;
    --foreground: #1f2937;
    --muted-foreground: #6b7280;
    --card-bg: rgba(255, 255, 255, 0.8);
    --border-color: #e5e7eb;

    /* Efek Kaca dengan Border Halus */
    --glass-bg: rgba(255, 255, 255, 0.6);
    --glass-border: 1px solid rgba(255, 255, 255, 0.8);

    /* Shadow yang lebih artistik */
    --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.05);
    --shadow-md: 0 4px 15px rgba(0, 0, 0, 0.07);
    --shadow-lg: 0 10px 30px rgba(0, 0, 0, 0.1);
}

.dark {
    --primary-rgb: 167, 139, 250; /* Untuk digunakan di rgba() */
    --primary: #a78bfa;
    --primary-light: #c4b5fd;
    --primary-dark: #8b5cf6;
    --primary-foreground: #111827;

    --secondary: #2dd4bf;
    --success: #4ade80;
    --danger: #f87171;

    /* Tema Dark */
    --background: #0d1117;
    --foreground: #e5e7eb;
    --muted-foreground: #9ca3af;
    --card-bg: rgba(22, 27, 34, 0.8);
    --border-color: #30363d;

    /* Efek Kaca dengan Border Halus */
    --glass-bg: rgba(22, 27, 34, 0.65);
    --glass-border: 1px solid rgba(255, 255, 255, 0.1);
}

/* ======================================= */
/* GENERAL & CORE LAYOUT                   */
/* ======================================= */

body {
    margin: 0;
    padding: 0;
    font-family: var(--font-body);
    color: var(--foreground);
    transition: background-color 0.5s, color 0.5s;
    background: linear-gradient(-45deg, #ee7752, #e73c7e, #23a6d5, #23d5ab);
    background-size: 400% 400%;
    animation: aurora-gradient 15s ease infinite;
}

.header-main {
    position: fixed; top: 0; left: 0; right: 0;
    display: flex; justify-content: space-between; align-items: center;
    padding: 1rem 1.25rem;
    z-index: 40;
    background: var(--glass-bg);
    backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px);
    border-bottom: var(--glass-border);
    color: var(--foreground);
    transition: all 0.3s ease-in-out;
}

.header-main h1 { font-family: var(--font-display); font-weight: 700; }
.main-content { padding-top: 85px; padding-bottom: 90px; }

.bottom-nav {
    position: fixed; bottom: 0; left: 0; right: 0;
    background: var(--glass-bg);
    backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px);
    border-top: var(--glass-border);
    z-index: 40;
    box-shadow: 0 -5px 20px rgba(0,0,0,0.08);
}

.nav-btn { color: var(--muted-foreground); transition: color 0.2s, transform 0.2s; }
.nav-btn.active, .nav-btn:hover { color: var(--primary); transform: translateY(-2px); }
.nav-btn:active { transform: scale(0.95); }
.nav-btn span { font-family: var(--font-display); font-weight: 500; }

/* ======================================= */
/* HOMEPAGE ELEMENTS                       */
/* ======================================= */

.hero-banner-container {
    display: flex; overflow-x: auto; scroll-snap-type: x mandatory;
    margin-top: -85px; scrollbar-width: none; -ms-overflow-style: none;
}
.hero-banner-container::-webkit-scrollbar { display: none; }
.banner-item { flex: 0 0 100%; scroll-snap-align: start; aspect-ratio: 16 / 10; }
.banner-item img { width: 100%; height: 100%; object-fit: cover; }

.homepage-content-wrapper {
    background-color: var(--background);
    border-radius: 2rem 2rem 0 0;
    padding-top: 1.5rem;
    padding-bottom: 1rem;
    margin-top: -2rem;
    position: relative;
    z-index: 5;
}

.saldo-card {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: var(--card-bg);
    backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px);
    box-shadow: var(--shadow-lg);
    border-radius: 1.5rem;
    padding: 1rem 1.25rem;
    margin: 0 1rem 1.5rem 1rem;
    position: relative; overflow: hidden;
    transition: transform 0.4s ease, box-shadow 0.4s ease;
}
.saldo-card::before {
    content: ''; position: absolute; top: 0; left: -150%;
    width: 100%; height: 100%;
    background: linear-gradient(110deg, rgba(255,255,255,0) 40%, rgba(255,255,255,0.1) 50%, rgba(255,255,255,0) 60%);
    transform: skewX(-25deg); transition: left 0.8s ease;
}
.saldo-card:hover { transform: translateY(-8px) scale(1.02); box-shadow: 0 20px 40px rgba(0,0,0,0.15); }
.saldo-card:hover::before { left: 150%; }
.saldo-info, .saldo-actions { z-index: 2; position: relative; }
.saldo-label { color: var(--muted-foreground); font-weight: 500; font-size: 0.8rem; }
.saldo-amount {
    font-family: var(--font-display);
    color: var(--foreground);
    font-size: 1.5rem;
    font-weight: 600;
    text-shadow: 0 1px 2px rgba(0,0,0,0.1);
    margin: 0;
}
.saldo-actions { display: flex; gap: 0.75rem; }
.action-btn {
    display: flex; flex-direction: column; align-items: center;
    gap: 0.3rem; cursor: pointer; color: var(--muted-foreground); transition: color 0.2s;
}
.action-btn:hover { color: var(--primary); }
.action-btn i { font-size: 1.4rem; transition: transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275); }
.action-btn span { font-size: 0.7rem; font-weight: 500; }
.action-btn:hover i { transform: scale(1.3) rotate(-10deg); }

.menu-grid {
    display: grid; grid-template-columns: repeat(auto-fill, minmax(80px, 1fr));
    gap: 1.25rem; padding: 0 1.5rem; margin-top: 1.5rem;
}
.menu-card { text-align: center; font-size: 0.8rem; font-weight: 600; cursor: pointer; transition: transform 0.3s ease; }
.menu-card:hover { transform: translateY(-5px); }
.feature-icon {
    width: 60px; height: 60px; border-radius: 1.25rem;
    margin: 0 auto 0.75rem auto; display: flex; align-items: center; justify-content: center;
    color: white; font-size: 1.75rem;
    transition: all 0.4s cubic-bezier(0.25, 1, 0.5, 1);
    box-shadow: var(--shadow-md);
}
.menu-card:hover .feature-icon { transform: scale(1.15) rotate(10deg); border-radius: 50%; box-shadow: var(--shadow-lg); }

.homepage-section {
    padding: 1.5rem 1rem;
}
.section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
    padding: 0 0.5rem;
}
.section-title {
    font-family: var(--font-display);
    font-size: 1.25rem;
    font-weight: 700;
    margin: 0;
}
.section-see-all {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--primary);
    text-decoration: none;
}

.promo-carousel {
    display: flex;
    gap: 1rem;
    overflow-x: auto;
    padding: 0.5rem 0.5rem 1.5rem 0.5rem;
    scroll-snap-type: x mandatory;
    scrollbar-width: none;
    -ms-overflow-style: none;
}
.promo-carousel::-webkit-scrollbar {
    display: none;
}
.promo-card {
    flex: 0 0 70%;
    max-width: 280px;
    height: 160px;
    position: relative;
    border-radius: 1rem;
    overflow: hidden;
    scroll-snap-align: start;
    box-shadow: var(--shadow-lg);
    transition: transform 0.3s ease;
}
.promo-card:hover {
    transform: translateY(-5px);
}
.promo-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}
.promo-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 1rem;
    background: linear-gradient(to top, rgba(0,0,0,0.8), transparent);
    color: white;
}
.promo-tag {
    background-color: var(--primary);
    color: var(--primary-foreground);
    padding: 0.25rem 0.5rem;
    border-radius: 0.375rem;
    font-size: 0.7rem;
    font-weight: 600;
}
.promo-title {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: 0.875rem;
    margin: 0.5rem 0 0 0;
}

.why-us-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
    text-align: center;
}
.why-us-item {
    background: var(--card-bg);
    border: 1px solid var(--border-color);
    border-radius: 1rem;
    padding: 1rem;
}
.why-us-item i {
    font-size: 1.75rem;
    color: var(--primary);
    margin-bottom: 0.75rem;
}
.why-us-item h4 {
    font-family: var(--font-display);
    font-size: 0.875rem;
    font-weight: 600;
    margin: 0 0 0.25rem 0;
}
.why-us-item p {
    font-size: 0.75rem;
    color: var(--muted-foreground);
    margin: 0;
}

.relative {
    position: relative;
}
.autocomplete-suggestions {
    position: absolute;
    width: 100%;
    background-color: var(--card-bg);
    border: 1px solid var(--border-color);
    border-radius: 0.75rem;
    margin-top: 0.5rem;
    max-height: 220px;
    overflow-y: auto;
    z-index: 50;
    box-shadow: var(--shadow-lg);
    backdrop-filter: blur(5px);
    -webkit-backdrop-filter: blur(5px);
}
.suggestion-item {
    padding: 0.85rem 1rem;
    cursor: pointer;
    font-weight: 500;
    transition: background-color 0.2s;
}
.suggestion-item:not(:last-child) {
    border-bottom: 1px solid var(--border-color);
}
.suggestion-item:hover {
    background-color: rgba(var(--primary-rgb), 0.1);
    color: var(--primary);
}

/* ======================================= */
/* UI COMPONENTS                           */
/* ======================================= */

.btn-primary {
    position: relative; overflow: hidden;
    display: flex; align-items: center; justify-content: center;
    background: linear-gradient(45deg, var(--primary), var(--primary-dark));
    color: var(--primary-foreground); padding: 0.9rem 1.5rem; border-radius: 0.75rem;
    font-weight: 600; text-align: center; transition: all 0.3s ease;
    border: none; box-shadow: 0 4px 15px -5px var(--primary-dark);
    transform: translateY(0);
}
.btn-primary:hover { transform: translateY(-4px); box-shadow: 0 8px 25px -8px var(--primary-dark); }
.btn-primary .ripple { position: absolute; border-radius: 50%; background-color: rgba(255, 255, 255, 0.6); transform: scale(0); animation: ripple 0.6s linear; }

.form-section {
    background-color: var(--card-bg);
    backdrop-filter: blur(5px); -webkit-backdrop-filter: blur(5px);
    padding: 1.5rem; border-radius: 1.25rem;
    border: 1px solid var(--border-color); box-shadow: var(--shadow-md);
    margin: 0 1rem 1.5rem 1rem; animation: fade-in 0.5s ease-out;
}
.modern-input, .modern-select {
    width: 100%; padding: 0.9rem 1rem; border: 1px solid var(--border-color);
    background-color: var(--background); border-radius: 0.5rem;
    transition: border-color 0.2s, box-shadow 0.2s; color: var(--foreground); font-size: 1rem;
}
.modern-input:focus, .modern-select:focus { outline: none; border-color: var(--primary); box-shadow: 0 0 0 3px var(--primary-light); }

.modal-wrapper {
    position: fixed; inset: 0; z-index: 50;
    display: flex; justify-content: center; align-items: flex-end;
    transition: opacity 0.3s, visibility 0.3s;
}
.modal-wrapper.hidden { opacity: 0; visibility: hidden; pointer-events: none; }
.modal-wrapper.active { opacity: 1; visibility: visible; pointer-events: all; }
.modal-overlay { position: absolute; inset: 0; background-color: rgba(0, 0, 0, 0.6); backdrop-filter: blur(4px); }
.modal-content {
    background-color: var(--background); border-radius: 1.5rem 1.5rem 0 0;
    padding: 1.5rem; padding-top: 1rem; width: 100%; max-width: 500px;
    box-shadow: 0 -10px 40px rgba(0,0,0,0.2); transform: translateY(100%);
    transition: transform 0.4s cubic-bezier(0.25, 1, 0.5, 1); position: relative;
}
.modal-wrapper.active .modal-content { transform: translateY(0); }
.modal-header { padding-bottom: 1rem; margin-bottom: 1rem; border-bottom: 1px solid var(--border-color); }

.counter-btn {
    width: 2.5rem; height: 2.5rem; border-radius: 50%; border: none;
    background-color: rgba(var(--primary-rgb), 0.1); font-size: 1.5rem; font-weight: 400; color: var(--primary);
    transition: background-color 0.2s, color 0.2s; display: flex; align-items: center; justify-content: center;
}
.counter-btn:hover { background-color: var(--primary); color: var(--primary-foreground); }
.counter-value { font-size: 1.25rem; font-weight: 600; min-width: 2rem; text-align: center; }

.notification-toast {
    position: fixed; bottom: 2rem; left: 50%;
    transform: translateX(-50%) translateY(150%);
    background-color: #2d3748; color: white;
    padding: 0.75rem 1.25rem; border-radius: 0.75rem;
    box-shadow: var(--shadow-lg); z-index: 1000; font-weight: 500;
    transition: transform 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275), opacity 0.5s;
    opacity: 0;
}
.notification-toast.show { transform: translateX(-50%) translateY(0); opacity: 1; }

#sideMenu {
    position: fixed; top: 0; left: -100%; bottom: 0;
    width: 80%; max-width: 320px;
    background: var(--glass-bg);
    backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
    border-right: var(--glass-border);
    color: var(--foreground); z-index: 100;
    transition: left 0.4s cubic-bezier(0.25, 1, 0.5, 1);
    box-shadow: 10px 0 50px rgba(0,0,0,0.2);
}
.side-menu-item {
    display: flex; align-items: center; gap: 1rem;
    padding: 0.9rem 1.25rem; border-radius: 0.75rem; font-weight: 500;
    margin: 0.25rem 1rem; transition: background-color 0.2s, transform 0.2s;
}
.side-menu-item:hover { background-color: rgba(var(--primary-rgb), 0.1); transform: translateX(5px); color: var(--primary); }
.side-menu-item i { width: 1.5rem; text-align: center; font-size: 1.1rem; opacity: 0.8; }

/* ======================================= */
/* CARDS & LISTS                           */
/* ======================================= */

.product-card, .product-card-list, .flight-result-card, .news-card {
    background-color: var(--card-bg);
    backdrop-filter: blur(5px); -webkit-backdrop-filter: blur(5px);
    border: 1px solid var(--border-color);
    border-radius: 1rem;
    box-shadow: var(--shadow-md);
    transition: border-color 0.3s, transform 0.3s, box-shadow 0.3s;
    padding: 1rem; margin-bottom: 1rem; display: block;
}
.product-card:hover, .product-card-list:hover, .flight-result-card:hover, .news-card:hover {
    border-color: var(--primary-light);
    transform: translateY(-6px);
    box-shadow: var(--shadow-lg);
}
.flight-result-card { display: flex; align-items: center; gap: 1rem; }

/* ======================================= */
/* PAGE SPECIFIC STYLING                   */
/* ======================================= */

.pulsa-menu-item, .nominal-btn, .payment-method-card {
    border: 2px solid var(--border-color);
    background-color: var(--background);
    border-radius: 0.75rem;
    padding: 1rem;
    transition: all 0.2s ease-in-out;
    cursor: pointer;
    text-align: center;
}
.pulsa-menu-item:hover, .nominal-btn:hover, .payment-method-card:hover {
    border-color: var(--primary-light);
    transform: translateY(-4px);
    box-shadow: var(--shadow-md);
}
.pulsa-menu-item.active, .nominal-btn.active {
    border-color: var(--primary);
    background-color: rgba(var(--primary-rgb), 0.1);
    box-shadow: 0 0 0 3px rgba(var(--primary-rgb), 0.3);
}
.pulsa-menu-item p, .nominal-btn p { font-weight: 600; }

.tab-container { display: flex; border: 1px solid var(--border-color); border-radius: 0.75rem; overflow: hidden; }
.tab-btn { flex: 1; padding: 0.75rem; border: none; background-color: transparent; font-weight: 600; cursor: pointer; color: var(--muted-foreground); transition: all 0.2s ease-in-out; }
.tab-btn.active { background-color: var(--primary); color: var(--primary-foreground); }

.status-badge { padding: 0.25rem 0.5rem; border-radius: 9999px; font-size: 0.75rem; font-weight: 600; }
.status-success { background-color: rgba(22, 163, 74, 0.1); color: var(--success); }

.contact-icon { display: flex; flex-direction: column; align-items: center; gap: 0.5rem; color: var(--foreground); font-weight: 600; font-size: 0.875rem; text-decoration: none; transition: transform 0.2s; }
.contact-icon:hover { transform: scale(1.1); color: var(--primary); }
.contact-icon .icon-wrapper { width: 4rem; height: 4rem; display: flex; align-items: center; justify-content: center; border-radius: 50%; background-color: rgba(var(--primary-rgb), 0.08); transition: background-color 0.2s; }
.contact-icon:hover .icon-wrapper { background-color: rgba(var(--primary-rgb), 0.15); }
.contact-icon .icon-wrapper i { font-size: 2rem; color: var(--primary); }

.faq-item { background: var(--background); padding: 0.75rem 1rem; border-radius: 0.75rem; border: 1px solid var(--border-color); }
.faq-item summary { font-weight: 600; cursor: pointer; list-style: none; display: flex; justify-content: space-between; align-items: center; }
.faq-item summary::-webkit-details-marker { display: none; }
.faq-item summary::after { content: '\f078'; font-family: 'Font Awesome 5 Free'; font-weight: 900; transition: transform 0.2s; }
.faq-item[open] > summary::after { transform: rotate(180deg); }
.faq-item p { margin-top: 0.75rem; color: var(--muted-foreground); font-size: 0.875rem; padding-bottom: 0.25rem; }

.settings-section {
    background-color: var(--card-bg);
    border-radius: 1.25rem;
    overflow: hidden;
    border: 1px solid var(--border-color);
    box-shadow: var(--shadow-sm);
}
.settings-item {
    display: flex; justify-content: space-between; align-items: center;
    padding: 1rem 1.25rem;
    transition: background-color 0.2s;
    cursor: pointer;
}
.settings-item:not(:last-child) { border-bottom: 1px solid var(--border-color); }
.settings-item:hover { background-color: rgba(var(--primary-rgb), 0.05); }

.toggle-switch { position: relative; display: inline-block; width: 44px; height: 24px; }
.toggle-switch input { opacity: 0; width: 0; height: 0; }
.toggle-slider {
    position: absolute; cursor: pointer;
    top: 0; left: 0; right: 0; bottom: 0;
    background-color: var(--border-color);
    border-radius: 24px; transition: 0.3s;
}
.toggle-slider:before {
    position: absolute; content: "";
    height: 18px; width: 18px;
    left: 3px; bottom: 3px;
    background-color: white;
    border-radius: 50%; transition: 0.3s;
}
input:checked + .toggle-slider { background-color: var(--primary); }
input:checked + .toggle-slider:before { transform: translateX(20px); }

.prose h4 {
    font-family: var(--font-display);
    color: var(--secondary);
    border-bottom: 2px solid var(--border-color);
    padding-bottom: 0.5rem;
    margin-top: 1.5rem;
}
.prose ul {
    list-style-type: disc;
    padding-left: 1.5rem;
}
.prose li {
    margin-bottom: 0.5rem;
}

/* ======================================= */
/* SPLASH SCREEN - DENGAN LOGO ANDA        */
/* ======================================= */
/* Keyframes untuk animasi logo (fade in & zoom) */

@keyframes fade-in-zoom {
    from {
        opacity: 0;
        transform: scale(0.8);
    }
    to {
        opacity: 1;
        transform: scale(1);
    }
}

.splash-screen {
    position: fixed;
    inset: 0; 
    z-index: 9999;
    display: flex;
    justify-content: center;
    align-items: center;
    /* Latar belakang putih bersih agar logo Anda terlihat menonjol */
    background-color: #ffffff;
    /* Transisi untuk efek menghilang yang halus */
    transition: opacity 0.8s ease-out, visibility 0.8s ease-out;
}

.splash-logo {
    /* Atur lebar logo, 65% ideal untuk sebagian besar layar ponsel */
    width: 65%; 
    max-width: 320px; /* Batas maksimal agar tidak terlalu besar di tablet */
    height: auto;
    /* Terapkan animasi masuk selama 1.2 detik */
    animation: fade-in-zoom 1.2s cubic-bezier(0.25, 1, 0.5, 1) forwards;
}

/* Kelas untuk menyembunyikan splash screen */
.splash-screen.hidden {
    opacity: 0;
    visibility: hidden;
}

